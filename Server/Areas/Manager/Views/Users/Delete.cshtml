@model Persistence.Entities.Users

<h3></h3>
<div>
    <div class="col-md-12">
        <div class="card text-white">
            <div class="card-header">اخطار</div>
            <div class="card-body">
                <h6 class="card-title">آیا از حذف کاربر "@Model.Name" اطمینان دارید</h6>
            </div>
        </div>
    </div>
    <hr />
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="بله" class="btn btn-outline-danger" />
            <button type="button" class="btn btn-outline-warning" data-dismiss="modal">خیر</button>
        </div>
    }
</div>
